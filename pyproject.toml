[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "stock-scanner-mcp-server"
version = "0.0.1"
description = "Local MCP server + fastmcp client shim for development"
requires-python = ">=3.8"
authors = [{name = "Vikas Satpute"}]
